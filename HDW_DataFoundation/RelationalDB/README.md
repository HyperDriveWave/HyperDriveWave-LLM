# HyperDriveWave RelationalDB

PostgreSQL metadata boundary for documents, chunks, sessions, feedback,
evaluation runs, and audit logs.

Run by mounting `init/` into the Postgres container:

```text
/docker-entrypoint-initdb.d
```

