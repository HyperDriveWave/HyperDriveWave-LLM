fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2
httpx>=0.27

# 趋势图不在后端出图：前端用内联 SVG 绘制（见 industrial-webui/lib/trend-chart.js）。
# 这样矢量自适应、可交互，也不必把 base64 图片塞进 JSON——
# 一张 PNG 会让响应体大一个量级（实测 71KB vs SVG 5KB）。
